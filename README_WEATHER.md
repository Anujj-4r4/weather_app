# Weather App — Notes

Files added/edited:
- `src/components/Search.jsx` — search input component
- `src/components/WeatherCard.jsx` — current weather UI
- `src/components/Forecast.jsx` — 5-day forecast summary
- `src/api/weather.js` — OpenWeatherMap wrapper (uses `VITE_OPENWEATHER_API_KEY`)

To run:
1. npm install
2. Copy `.env.example` -> `.env` and set your API key
3. npm run dev

If you'd like, I can now install packages and run the dev server for you. Reply "install" to proceed.